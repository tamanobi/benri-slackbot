{
    "version": 2,
    "builds": [
        {
            "src": "index.py",
            "use": "@now/python"
        }
    ],
    "env": { "SLACK_WEBHOOK": "@slack_webhook" },
    "routes": [{ "src": "/.*", "dest": "/" }]
}
